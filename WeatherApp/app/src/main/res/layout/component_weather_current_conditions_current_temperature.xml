<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_gravity="center_horizontal"
    android:layout_height="wrap_content"
    android:layout_marginTop="16dp"
    android:layout_width="wrap_content"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/iv_fragment_weather_current_conditions_cloudiness"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_gravity="center"
        android:contentDescription="@string/cloudiness_icon"
        android:src="@drawable/ic_launcher_foreground"/>

    <TextView
        android:id="@+id/tv_fragment_weather_current_conditions_temperature"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="16dp"
        android:paddingEnd="0dp"
        android:textSize="80sp" />

    <ImageView
        android:id="@+id/iv_fragment_weather_current_conditions_temperature_units"
        android:layout_gravity="center"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:contentDescription="@string/units_icon"
        android:src="@drawable/ic_launcher_foreground"/>

</LinearLayout>